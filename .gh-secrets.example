# Grafana Cloud credentials for Vector observability pipeline.
# Copy this file to .gh-secrets and fill in your values.
#
# To find these values:
#   1. Go to grafana.com -> My Account -> your stack
#   2. Click Prometheus -> Details for PROMETHEUS_URL and PROMETHEUS_USER
#   3. Click Loki -> Details for LOKI_URL and LOKI_USER
#   4. Go to Security -> Access Policies -> create a policy with metrics:write and logs:write
#   5. Create a token for that policy -> GRAFANA_API_KEY
#
# Load into GitHub: ./scripts/set-gh-secrets.sh
# Verify locally:   ./scripts/verify-grafana-secrets.sh

GRAFANA_PROMETHEUS_URL=https://prometheus-prod-XX-prod-us-east-X.grafana.net/api/prom/push
GRAFANA_PROMETHEUS_USER=YOUR_PROMETHEUS_INSTANCE_ID
GRAFANA_LOKI_URL=https://logs-prod-XXX.grafana.net
GRAFANA_LOKI_USER=YOUR_LOKI_INSTANCE_ID
GRAFANA_API_KEY=glc_YOUR_ACCESS_POLICY_TOKEN

# Grafana Cloud credentials for Terraform-managed dashboards & alerts.
# These are separate from the Vector pipeline credentials above.
#
# To find these values:
#   1. Go to your Grafana Cloud stack URL (e.g. https://<stack>.grafana.net)
#   2. Go to Administration -> Service Accounts -> create account with Editor role
#   3. Generate a token for that service account -> GRAFANA_SA_TOKEN
#   4. GRAFANA_URL is your stack URL (e.g. https://<stack>.grafana.net)
#   5. Go to Connections -> Data sources -> click each data source -> copy the UID from the URL
#      (e.g. https://<stack>.grafana.net/connections/datasources/edit/<UID>)
GRAFANA_URL=https://YOUR_STACK.grafana.net
GRAFANA_SA_TOKEN=glsa_YOUR_SERVICE_ACCOUNT_TOKEN
GRAFANA_PROMETHEUS_UID=YOUR_PROMETHEUS_DATA_SOURCE_UID
GRAFANA_LOKI_UID=YOUR_LOKI_DATA_SOURCE_UID
