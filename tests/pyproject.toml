[project]
name = "freeradius-tests"
version = "0.1.0"
description = "Integration tests for FreeRADIUS lab"
requires-python = ">=3.11"
dependencies = [
    "pyrad>=2.4",
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["."]
addopts = "-v --tb=short"
